using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// the enemy needs to walk left and right randomly and stop when it senses the player within its radius and
// start shooting the player
// when the player hits the enemy 5 itmes, then the enemy gets deleted and a new one gets generated from the
// exact same initial location 
 


public class enemy_movement : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
